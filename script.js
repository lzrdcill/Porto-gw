console.log("Website Jasa Digital aktif!");

// Efek kursor berubah saat hover pada elemen interaktif
document.querySelectorAll(".card-hover").forEach(card => {
  card.style.cursor = "pointer";
});
